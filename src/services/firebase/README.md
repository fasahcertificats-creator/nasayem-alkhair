# Firebase Service Layer

Firebase SDK access is centralized here. Next.js pages, layouts, and feature presentation components must not import Firebase SDK modules directly.

Future feature infrastructure implementations can depend on these helpers to access Auth, Firestore, Storage, and Messaging.
